/**
 * Window -> é o objeto pai no HTML
 * document HTML -> seguindo a ordem document filho do window
 * heade -> filho do document
 * body -> filho do html
 * 
 * header -> Não aparece no modelo mas pode existir
 * main -> Não aparece no modelo mas pode existir
 * footer -> Não aparece no modelo mas pode existir
 * 
 * section -> filho do body no modelo de exemplo
 * h1 -> filho do section
 * conteúdo do h1 -> filho do h1
 */